# 代码审查与精简优化状态

## 约束与状态

- 本文件是本 Goal 唯一的进度来源；`.work/code-optimization/` 仅保存快照、哈希与原始验证输出。
- 状态仅使用：`pending / in_progress / validating / completed / rejected / blocked`。
- 优先级：正确性与安全风险、收益、证据强度、改动风险、验证成本。
- 不读取 `nas/data`，不新增生产依赖，不修改真实数据库或生产配置。

## 当前基线

采集时间：2026-07-28 11:32（Asia/Shanghai）。

非 Git 工作区保护：

- 有效只读快照：`.work/code-optimization/baseline-20260728-113053/`
- 快照文件数：280
- `SHA256SUMS.txt` 自身 SHA-256：`c8626814d16d4ff7b8a702f69666089d2fea664323758a0961e990d36c09c553`
- `.work/code-optimization/baseline-20260728-113021/` 是首次命令留下的扁平化只读原始快照；保留但不作为恢复基线。

采集命令：

```powershell
git rev-parse --is-inside-work-tree
Get-ChildItem src -Recurse -File # 限定 .ts/.vue/.css/.html 后统计字节、物理行、非空行和行长
pnpm verify
Get-ChildItem dist/web,dist/server -Recurse -File # 统计构建产物
```

| 指标 | baseline |
|---|---:|
| `src` TS/Vue/CSS/HTML 文件 | 126 |
| 总字节 | 635,233 |
| 物理行 | 12,641 |
| 非空行 | 11,841 |
| 行长 > 120 / 160 / 300 / 1000 | 690 / 284 / 67 / 4 |
| Web JS/CSS/HTML 构建字节 | 400,627 |
| Web 全部构建字节（含图片、map） | 3,214,487 |
| API entry / Worker entry / Server shared chunk | 174,410 / 40,848 / 148,395 |
| Server JS 总字节 | 363,653 |
| 测试 | 39 文件、140 用例通过；6 文件、15 用例跳过；Vitest 2.88s |
| lint | 0 error，970 warning |
| typecheck / build | 通过 / 通过 |

基线门禁原始输出：`.work/code-optimization/baseline-20260728-113053-verify.log`。跳过的数据库/运行时验收不计为通过，后续涉及相应路径时需使用隔离测试数据库补证。

## 候选表

| ID | 文件 | 根因与证据 | 价值 | 风险 | 预计收益 | 状态 |
|---|---|---|---|---|---|---|
| DB-001 | `src/db/database.ts`, `src/db/pool.ts` | 两个通用事务入口各自实现 `BEGIN/COMMIT/ROLLBACK/release`；`queryOne` 全仓仅命中定义。复核发现 `Transaction` 仍被 `src/db/outbox.ts` 使用，已保留 | 中 | 低 | 通用事务生命周期实现 2→1，删除死辅助代码，降低以后修改扩散面 | `completed` |
| API-SEC-001 | `src/api/routes/auth.ts`, `src/api/service-graph.ts` | 两套认证实现漂移：auth 写路由把空 CSRF 字符串传给 service，而 service 将空串视为未提供；logout/change-phone 可绕过 CSRF 比较 | 高 | 低 | 恢复写请求 CSRF 契约，认证实现 2→1 | `completed` |
| API-001 | `src/api/routes/imports.ts`, `reports.ts`, `src/api/service-graph.ts` | 路由认证后只保留 account id，授权适配器又 `findAccountById` 重建同一 Actor；9 个端点各多 2 次账户/角色查询 | 高 | 中 | 相关请求认证后查询 7→5（-28.6%），删除两套重复适配 | `completed` |
| API-002 | `src/api/service-graph.ts` | 店铺能力授权先查 shop、再串行查 membership；`(shop_id, account_id)` 有唯一约束 | 高 | 中 | 授权查询往返 2→1；需证明 owner/admin/customer/不存在店铺语义不变 | `completed` |
| API-003 | `src/api/routes/*` | 幂等键提取重复，但当前长度、空白和字段文案契约不同 | 中 | 中 | 仅在确认统一契约后删除重复；否则拒绝抽象 | `completed` |
| API-004 | `src/api/service-graph.ts` | imports/reports 的 action→capability 与认证/授权装配重复 | 中 | 中 | 降低新增动作的修改扩散面，不隐藏权限差异 | `completed` |
| WEB-001 | `src/modules/shops/service.ts`, `src/web/api/client.ts` | 服务端 `ShopView` 已返回 status/term 字段，Web 又维护 `ShopWire/mapShop` 同一状态与日期映射，6 组真实调用 | 中 | 低 | 删除第二状态转换事实来源和 6 组运行时映射 | `completed` |
| WEB-002 | `src/web/pages/*`, `useAsyncResource.ts`, `AsyncState.vue` | 多页疑似重复 busy/error/reload/action 状态 | 中 | 中 | 至少两个稳定同义调用点成立时复用；避免万能 composable | `rejected` |
| WEB-003 | `src/web/pages/ExportPage.vue`, `ResultsPage.vue`, `src/web/navigation.ts` | 两页重复店铺 capability 判断，且未覆盖 TRASHED/EXPIRED/客户无快照分支 | 中 | 低 | 复用已有 `canAccessShopPage`，删除第二套前端能力判断 | `completed` |
| READ-001 | `src/modules/exports/postgres.ts`, `calculation/postgres-runner.ts`, Vue 页面 | 4 行超过 1000 字符，另有 63 行超过 300 字符；lint 970 warning | 中 | 低至中 | 恢复可读性；必须与财务/权限切片分离且不以增行伪装精简 | `completed` |
| SVC-001 | `shops/service.ts`, `memberships/service.ts`, `payments/postgres.ts`, `auth/postgres.ts` | 大型服务中疑似重复幂等、审计、状态切换和查询映射；只有支付新事件的两种业务重复分支被证明完全同义 | 中至高 | 高 | 只合并同事务、同更新、同钱包锁和同返回的支付重复终态；其余用反例拒绝 | `completed` |
| EXP-001 | `src/modules/exports/postgres.ts`, `export-report.ts` | 宽重构无收益；审查确认已提交密文可能被失败清理误删、创建/撤权 TOCTOU、CSV 数值契约漂移、FX 分区顺序与 manifest 类型错误；最终复核又证实 POSIX 工作目录、大数精度和错误泄漏 | 高 | 高 | 以独立生命周期/校验窄切片修复，保持流式、幂等和下载实时授权 | `completed` |
| SHOP-EXP-001 | `src/api/service-graph.ts`, `src/modules/shops/service.ts` | 到期转换只在店铺列表触发；直接调用上传/发布等 API 时持久状态仍可为 ACTIVE | 高 | 高 | 对象级授权必须按上海当地关闭日收敛为只读，不依赖前端先拉列表 | `completed` |
| INV-EXP-001 | `src/modules/memberships/service.ts` | 自然过期邀请仍为 PENDING，命中部分唯一索引并永久阻止同店铺手机号重邀 | 高 | 中 | 在同一事务持久化 EXPIRED、释放唯一键并写审计 | `completed` |
| SHOP-IDEM-001 | `src/modules/shops/service.ts`, `catalog/index.ts` | 店铺收费和价格版本只按幂等键复用，未完整绑定动作目标与规范请求 | 中至高 | 高 | 相同键不同店铺/动作/期限/价格生效时间必须冲突，不可静默返回旧结果 | `completed` |
| AUDIT-001 | `auth/*`, `payments/postgres.ts`, `exports/*`, report routes | 登录/失败、换绑、支付对账、导出及管理员财务访问的明确审计契约存在缺口；最终复核证实已消费但过期的换绑 OTP 仍可完成 | 高 | 中至高 | 按业务事务补窄审计，不做万能审计抽象，不记录手机号/OTP/token/原始数据 | `completed` |
| CLEAN-001 | `src/shared/*`, imports route 及零调用服务方法 | 私有仓库中存在成组零调用 schema/helper 和可选生产依赖回退，增加错误契约与修改扩散面 | 中 | 低 | 删除确定零调用实现；生产必需方法改为必填，不触碰外部契约 | `completed` |
| MEM-IDEM-001 | `src/modules/memberships/service.ts` | 幂等响应在当前对象级授权前返回；同值导出权限仍旋转 epoch，留下显示 SUCCEEDED 但实际不可下载的导出 | 中 | 中 | 重放先实时授权；同值变更成为不旋转 epoch 的 no-op | `completed` |
| API-REQ-001 | `src/api/routes/*` | 至少 26 个 UUID path、多个 query/date/numeric header 缺少运行时 Schema；非法值可进入 PostgreSQL cast 或 `BigInt()` 并成为 500 | 高 | 中 | 将无效输入稳定收敛为 400，避免无效数据库往返 | `completed` |
| REPORT-001 | `src/web/api/client.ts`, `ResultsPage.vue`, `src/api/routes/reports.ts` | Web 发送 start/end/marketplace，但 API 与服务忽略，结果筛选为 no-op | 高 | 中 | 恢复用户可观察的筛选契约；独立于精简切片 | `completed` |
| WEB-RACE-001 | `src/web/composables/useAsyncResource.ts` | 并发 reload 时旧响应可覆盖新响应；Results/UserAdmin 有真实调用点 | 高 | 低 | latest-response-wins，消除旧数据覆盖 | `completed` |

## 已完成与已拒绝

### DB-001：统一事务控制并删除死查询辅助

- 修改：`PostgresDatabase.transaction` 委托给 `withTransaction`，保留领域层 `SqlClient` 适配；删除全仓无调用的 `queryOne`。对抗复核发现 `Transaction` 被 outbox 使用，因此保留别名并修正候选证据。
- 行为保护：新增 `tests/unit/database.test.ts`，固定提交、回滚和连接释放顺序。
- 相关生产文件指标：2,743→2,271 字节（-472）；77→66 物理行（-11）；66→55 非空行（-11）。
- 验证：定向 4 文件/9 用例通过；全门禁 40 文件/142 用例通过，仍有 6 文件/15 用例跳过；lint 0 error/970 warning；typecheck、Web build、Server build 通过。全门禁原始输出：`.work/code-optimization/db-001-verify.log`。
- 性能结论：Vitest 2.88s→2.66s 属于单次运行噪声，不声称性能提升；本切片未改变 SQL 数量、事务边界或财务/权限语义。迁移、COPY、导入分析、复制恢复、FX 同步等专用事务有不同连接/阶段语义，未纳入本切片。

### API-SEC-001：收敛认证并拒绝空 CSRF

- 根因：`authRoutes` 只检查 CSRF header 是否为字符串，`AuthService.authenticate` 仅在 token truthy 时比较；空串因此跳过双提交 token 校验。其他写路由的 service graph 实现正确拒绝空串。
- 失败基线：新增回归后，logout/change-phone 两例均收到 200 而非 403，领域方法被调用。
- 修改：将认证、Origin、session cookie 和 CSRF 校验收敛到 `authenticateAuthRoute`；auth 写路由与 service graph 共同调用，公开 OTP/注册仍只执行原有 Origin 策略。
- 相关生产文件指标：17,398→16,935 字节（-463）；386→380 物理行（-6）；368→364 非空行（-4）。
- 验证：空 CSRF 两端点均返回 403 且认证/领域方法未调用；权限定向 3 文件/14 用例通过；全门禁 41 文件/144 用例通过，仍有 6 文件/15 用例跳过；lint 0 error/970 warning；typecheck 与构建通过。原始输出：`.work/code-optimization/api-sec-001-verify.log`。
- 两个已完成切片后的总指标：`src` 635,233→634,298 字节（-935），12,641→12,624 物理行（-17），11,841→11,826 非空行（-15）；Server JS 363,653→362,830 字节（-823），API entry 174,410→173,819（-591），Web 与 Worker entry 不变。

### API-001：保留 Actor 并删除重复账户重建

- 根因：imports/reports 在认证后把 Actor 降格为 account id，service graph 随即执行 `findAccountById`，固定增加 account 与 role 两次查询；两套适配器还重复 action→capability 映射。
- 修改：路由选项直接传递 `Actor` 与规范 `ShopCapability`；领域调用只读取 `actor.accountId`；删除两个账户重建适配器和 reports 未被实现使用的 actor 参数假契约。
- 行为保护：新增 `shop-route-actor-contract.test.ts`，固定认证 Actor、能力常量和领域 actorAccountId 的传递。
- 指标：本切片 `src` 634,298→633,132 字节（-1,166），12,624→12,611 物理行（-13），11,826→11,815 非空行（-11），>120 字符行 689→687。API entry 173,819→172,692 字节（-1,127）。
- 查询：由确定性调用图得出，9 个端点进入领域服务前从 session/account/roles 3 次 + 重建 account/roles 2 次 + shop/membership 2 次，变为 3+2，即 7→5（-28.6%）；未声称延迟百分比。
- 验证：定向 3 文件/12 用例通过，数据库验收文件另有 1 文件/3 用例因环境跳过。首次全门禁发现新测试 6 个 lint error，修正测试类型后重跑：42 文件/146 用例通过，6 文件/15 用例跳过；lint 0 error/970 warning；typecheck 与构建通过。原始输出：`.work/code-optimization/api-001-verify.log`（失败）与 `api-001-verify-rerun.log`（通过）。
- 对抗审查：能力映射与旧逻辑逐项一致；移除的第二次账户读取不提供事务级强一致性，且其他资源路由已使用认证 Actor。若未来要求角色变更与请求强一致，应由事务/会话世代契约解决，而不是依赖偶然 TOCTOU 重读。

### API-002：合并店铺与 membership 授权查询

- 失败基线：可计数回归中，customer 授权调用两次 query；不存在店铺为一次。
- 修改：利用 `shop_membership UNIQUE (shop_id, account_id)`，以 `shop LEFT JOIN shop_membership` 一次读取店铺和当前 Actor membership；授权决策仍由唯一 `authorizeShop` 实现。
- 查询：API-001 后相关端点认证前置 5 次查询进一步变为 4 次；相对原始 7 次减少 42.9%。本切片只声称确定性往返数下降，不声称 P95。
- 指标：本切片为恢复类型与 SQL 可读性，`src` 633,132→633,173 字节（+41），12,611→12,619 物理行（+8），11,815→11,823 非空行（+8），但 >120 字符行 687→686，且每次授权实际 query 2→1；API entry 172,692→172,661 字节（-31）。
- 验证：失败回归由 2 query 降为 1；权限定向 4 文件/14 用例通过；全门禁 43 文件/148 用例通过，6 文件/15 用例跳过，lint 0 error/970 warning，typecheck 与构建通过。原始输出：`.work/code-optimization/api-002-verify.log`。
- 未验证：环境未提供 `TEST_DATABASE_URL`，未运行真实 PostgreSQL `EXPLAIN (ANALYZE, BUFFERS)`；唯一索引与映射由迁移、可计数测试和既有授权单测证明，未据此声称数据库执行时间。

### WEB-RACE-001：异步资源 latest-response-wins

- 失败基线：两个定向用例稳定复现旧成功覆盖新成功、旧失败覆盖新成功；最终分别得到旧值和 error 状态。
- 修改：`useAsyncResource` 增加单调请求序号，仅当前最新请求可提交 data/status/error；不引入取消、通用 action 或新依赖。
- 指标：`src` 633,173→633,325 字节（+152），12,619→12,623 物理行（+4）；Web JS/CSS/HTML 400,627 baseline→400,673（累计 +46）。该切片以消除可观察竞态为收益，不声称体积优化；Goal 累计仍净精简。
- 验证：Web 9 文件/31 用例通过；全门禁 44 文件/150 用例通过，6 文件/15 用例跳过；lint 0 error/970 warning；typecheck 与构建通过。原始输出：`.work/code-optimization/web-race-001-verify.log`。
- 浏览器：结果页桌面 1440×900 与窄屏 390×844 均加载正式快照；连续触发筛选后指标仍可见，控制台无页面错误；窄屏 `documentScrollWidth=viewportWidth=375`，无页面级横向溢出。截图：`.work/code-optimization/web-race-001-desktop-1440x900.png`、`web-race-001-narrow-390x844.png`。验收后本地 API/Vite 进程已停止。

### WEB-001：删除重复店铺状态映射

- 根因：`ShopService.view` 已产生 Web 所需 `status/termStart/termEndExclusive`，`client.ts` 仍以 `ShopWire/mapShop` 重做状态和日期映射，覆盖 list/create/rename/renew/trash/restore 六组调用。
- 失败基线：让 `listShops` 接收规范响应的契约测试得到三个 `undefined` 字段，证明 Web 仍依赖第二套旧 wire。
- 修改：六组调用直接消费 `Shop`；删除 `ShopWire/mapShop`；同步把合成浏览器 fixture 改为服务端规范字段。
- 指标：本切片 `src` 633,325→632,263 字节（-1,062），12,623→12,596 物理行（-27），11,827→11,802 非空行（-25），>160 行 284→283，>300 行 67→66；Web JS/CSS/HTML 400,673→400,202 字节（-471）。
- 验证：定向 3 文件/8 用例通过；全门禁 44 文件/150 用例通过，6 文件/15 用例跳过；lint 0 error/970 warning；typecheck 与构建通过。原始输出：`.work/code-optimization/web-001-verify.log`。
- 浏览器：桌面与窄屏店铺页均显示 `ACTIVE` 与正确期限；窄屏无页面级横向溢出，控制台无错误/警告。截图：`.work/code-optimization/web-001-desktop-1440x900.png`、`web-001-narrow-390x844.png`；本地验收进程已停止。

### API-REQ-001（切片 1）：上传字节数在路由边界校验

- 失败基线：非法 `declaredSize` 与 `Upload-Offset` 均进入认证/授权或上传服务，随后由 `BigInt()` 抛错并返回 500；两个回归还证明了无效请求发生了不必要的下游调用。
- 修改：新增十进制非负整数字符串 Schema；`declaredSize` 上限 10 位，`Upload-Offset` 同上，`Content-Length` 上限 8 位，并保留 Fastify `bodyLimit` 作为实际请求体边界。非法值现由路由验证直接返回 400。
- 范围：此切片不改变分片协议、偏移语义、文件大小上限、认证授权或存储实现；API-REQ-001 仍保持 `in_progress`，其余 UUID、日期和查询参数另行处理。
- 指标：本切片 `src` 632,263→632,537 字节（+274），12,596→12,605 物理行（+9），11,802→11,810 非空行（+8）；>120 / >160 / >300 / >1000 字符行保持 686 / 283 / 66 / 4。收益是错误边界与无效下游调用，不声称体积精简。
- 验证：定向 3 文件/14 用例通过；全门禁 44 文件/152 用例通过，6 文件/15 用例跳过；lint 0 error/970 warning；typecheck 与 Web/Server 构建通过。API entry 172,930 字节。原始输出：`.work/code-optimization/api-req-001-upload-verify.log`。

### API-REQ-001（切片 2）：UUID 参数在路由边界校验

- 失败基线：upload batch/file、shop、application、account、export 与 export shop query 的代表性非法 UUID 均返回 200 或 500，并进入认证、资源解析或领域方法；新增的 5 个回归全部失败。
- 修改：复用 `src/shared/contracts.ts` 已有 `UuidSchema`，为审计发现的全部 UUID path 路由（34 个实际挂载变体）以及 export `shopId` query 增加 params/query Schema；imports/reports 原有 UUID Schema 保持不变。支付回调的 `provider` 不是 UUID，仍保留其明确 404 契约。
- 行为保护：覆盖 batch/file、shop、application、account、export id 与 export shop query；每类均验证 400 且认证/资源解析/领域方法未调用。
- 指标：本切片 `src` 632,537→634,011 字节（+1,474），12,605→12,623 物理行（+18），11,810→11,827 非空行（+17），>120 / >160 / >300 / >1000 为 687 / 284 / 66 / 4。API entry 172,930→174,982 字节（+2,052）。这是输入正确性切片，不声称体积收益；相对 Goal 基线 `src` 仍净减少 1,222 字节。
- 验证：定向 3 文件/13 用例通过；全门禁 45 文件/157 用例通过，6 文件/15 用例跳过；lint 0 error/970 warning；typecheck 与 Web/Server 构建通过。原始输出：`.work/code-optimization/api-req-001-uuid-verify.log`。

### API-REQ-001（切片 3）：查询参数在领域/数据库调用前校验

- 失败基线：非法原件下载 token 先读取文件并返回 500；FX history 非法日期/币种进入 service，生产 SQL 的 `$1::date` 会把非法日期变成数据库错误；imports completeness 的非法 shopId 进入认证与授权。
- 修改：原件下载 token 复用服务既有 43 字符 base64url 契约；FX history 使用 ISO date 与三字母币种列表 Schema；imports completeness 使用共享 UUID Schema。全部 UUID body/path/query Schema 统一引用 `UuidSchema`，删除路由内重复的 UUID 规则定义。
- 边界取舍：admin 用户搜索 query 只进入参数化 SQL 文本比较，且查询固定 `LIMIT 100`，没有类型转换或无界结果；在设计说明书没有长度契约时不擅自收紧。支付 callback `provider` 保留显式“不存在渠道”404，不改成 Schema 400。
- 指标：本切片 `src` 634,011→634,458 字节（+447），12,623→12,637 物理行（+14），11,827→11,842 非空行（+15），长行指标保持 687 / 284 / 66 / 4；API entry 174,982→175,175 字节（+193）。API-REQ-001 总体以正确性和减少无效调用为收益，不声称体积优化。
- 验证：失败回归从 200/500 变为 400，且 service/认证/授权未调用；定向 5 文件/17 用例通过；全门禁 46 文件/161 用例通过，6 文件/15 用例跳过；lint 0 error/970 warning；typecheck 与构建通过。原始输出：`.work/code-optimization/api-req-001-query-verify.log`。

### REPORT-001：恢复结果页日期与站点筛选

- 失败基线：route 丢弃 Web 已发送的 `start/end/marketplace`；倒置日期范围仍返回 200；report service 始终查询完整 `monthly_cost_summary`、完整费用与完整汇率回退，代表性回归均失败。
- 修改：route 对 ISO 日期、站点文本和 `start <= end` 做边界校验并传递 `ReportFilter`；report service 按事实的 `marketplace_local_date` 与规范站点过滤九项指标、费用和汇率回退，完整性矩阵按与日期范围相交的站点月份过滤。金额仍由持久化 `calculation_fact_result` 十进制定点结果聚合，平台结余使用 Decimal 按既有八项公式重算。
- 发布安全：带筛选的试算视图 `canPublish=false` 并给出清除筛选提示，避免 Web 将局部 completeness 当作完整发布清单；服务端原有 `PUBLISH_MANIFEST_NOT_FULL_SHOP` 防线保持不变。
- 指标：本切片 `src` 634,458→637,692 字节（+3,234），12,637→12,694 物理行（+57），11,842→11,897 非空行（+55）；>120 行 687→684，其他长行保持 284 / 66 / 4。API entry 175,175→175,996 字节（+821）。这是修复用户可观察错误与发布安全的功能切片，不声称体积优化。
- 验证：route/SQL 参数/可见月份/Decimal 平台结余/筛选发布禁用回归 4 文件/12 用例通过；全门禁 47 文件/163 用例通过，6 文件/15 用例跳过；lint 0 error/970 warning；typecheck 与构建通过。原始输出：`.work/code-optimization/report-001-verify.log`。`TEST_DATABASE_URL` 未设置，因此新增 SQL 未在真实 PostgreSQL 上执行或 EXPLAIN。
- 浏览器：桌面筛选 `2026-05-15..2026-05-31 + Amazon.com` 后完整性 3→1 行、费用 2→1 行；窄屏 390×844 的 `documentScrollWidth=viewportWidth=375`，页面日志只有 Vite debug。截图：`.work/code-optimization/report-001-desktop-1440x900.png`、`report-001-narrow-390x844.png`。验收 fixture 也改为遵守同一 query 合约，API/Vite 验收进程已停止。

### API-003：统一幂等键 HTTP 契约

- 失败基线：imports/reports 接受 5 字符键；exports 接受任意字符串；upload 对短键抛普通 Error 并返回 500。其余主流端点已使用 8..200，但重复实现。
- 修改：新增唯一 `requireIdempotencyKey`，统一非空白、8..200 字符、400、`IDEMPOTENCY_KEY_REQUIRED` 与 `Idempotency-Key` 字段；apps/admin 保留业务专属中文说明。shops、payments、apps、admin、imports、reports、uploads、exports 共 16 个命令入口改用同一实现。
- 行为保护：HTTP 回归覆盖 5 字符、8 个空格、201 字符拒绝，以及 8/200 边界接受；imports/reports 确认短键不会进入领域方法，upload 从 500 收敛为 400。
- 指标：本切片 `src` 637,692→636,663 字节（-1,029），12,694→12,677 物理行（-17），11,897→11,882 非空行（-15），>300 行 66→65；新增一个被 8 个 route 模块使用的 13 行边界文件。API entry 175,996→174,565 字节（-1,431）。
- 验证：定向 4 文件/14 用例通过；全门禁 48 文件/165 用例通过，6 文件/15 用例跳过；lint 0 error/970 warning；typecheck 与构建通过。原始输出：`.work/code-optimization/api-003-verify.log`。
- API-004 结论：其候选重复已随 API-001 一并消除——route 直接传 Actor 与规范 `ShopCapability`，service graph 不再维护 imports/reports 的 action→capability 适配器；没有剩余等价实现可继续抽象，因此按已完成而非新增抽象处理。

### WEB-002：拒绝通用 action composable

- 调用图结论：`useAsyncResource` 的稳定语义是只读资源加载、latest-response-wins、ready/empty/error；现有 action 状态分别包含 OTP 倒计时、头像 saved 状态、主题乐观回滚、上传断点/进度状态机、发布替换当前结果、导出后 reload、充值后刷新会话等不同生命周期与副作用。
- 取舍：只有 `busy + error` 的表面形状相似，成功提交、并发策略、错误落点和返回值提交均不同；引入万能 `useAsyncAction` 会把这些差异藏进配置并增加跳转，不会稳定减少代码或修改扩散面。因此候选 `rejected`，保留已验证的资源加载抽象。

### WEB-003：复用已有店铺 capability 判断

- 根因：router 已用 `canAccessShopPage` 统一处理 ACTIVE/EXPIRED/TRASHED、客户只读、正式快照与客户导出授权；Export/Results 又以 `access !== CUSTOMER` 等局部条件维护第二套按钮可见性，遗漏部分状态。
- 修改：两个页面直接对选中店铺调用 `canAccessShopPage(..., "EXPORT" | "MANAGE_IMPORT")`；删除局部 capability 分支，不改变 API 对象级授权。
- 指标：`src` 636,663→636,596 字节（-67），12,677→12,674 物理行（-3），11,882→11,879 非空行（-3）；Web JS/CSS/HTML 400,202→400,127 字节（-75）。
- 验证：navigation 的 OWNER/ADMIN/CUSTOMER、ACTIVE/EXPIRED/TRASHED、快照与导出授权 3 文件/8 用例通过；全门禁 48 文件/165 用例通过，6 文件/15 用例跳过；lint 0 error/970 warning；typecheck 与构建通过。原始输出：`.work/code-optimization/web-003-verify.log`。

### READ-001：消除极端长行并窄化 Vue 排版债务

- 范围：只机械排版 `AppAdminPage.vue`、`UserAdminPage.vue`，并仅展开 `calculation/postgres-runner.ts` 的交易事实对象与 `exports/postgres.ts` 的导出报告对象；没有改写 SQL、公式、权限、事务、流式分页或对象提交路径。为浏览器验收将 `tests/web/acceptance-api.ts` 的应用列表 fixture 对齐当前 `/api/v1/apps` 合约，并删除该 fixture 中已无调用的旧 `/api/v1/admin/apps` 分支。
- 等价证据：Vue formatter 首次引入 8 处标签首尾空白后被逐一纠正；使用项目锁定的 `@vue/compiler-sfc` 对基线与当前两个页面执行 `compileTemplate(..., whitespace: "condense")`，两份生成代码分别逐字相等（8,899 / 10,342 字节）。两个管理页在验收 API 下均完成加载且浏览器日志无应用错误；前后截图保留于 `.work/code-optimization/read-001-*-before.png` 与 `read-001-*-after.png`，但因 in-app viewport 在标签切换时改变，不把像素 diff 当作等价证据。
- 指标：`src` 636,596→640,154 字节（+3,558），12,674→12,914 物理行（+240），11,879→12,119 非空行（+240）；>120 / 160 / 300 / 1000 行由 684 / 284 / 65 / 4 降至 679 / 277 / 60 / 0。两页 lint warning 122→16，全仓 970→864；保留的 16 条仅来自为保持原始文本节点而拒绝 formatter 首尾空白的标签换行规则。Web JS/CSS/HTML 仍为 400,127 字节。
- 验证：全门禁 48 文件/165 用例通过，6 文件/15 用例跳过；lint 0 error/864 warning；typecheck 与构建通过。原始输出：`.work/code-optimization/read-001-verify.log`。该切片以可读性和审查面为收益，不声称源文件体积减少。

### SVC-001：只合并完全同义的支付重复终态

- 调用图结论：shop create/renew 的授权、期限 supersede、扣款时序与审计前后态不同；membership 导出授权与撤权的 artifact 失效、no-op、scope 和审计不同；auth 的主题/头像更新只具表面 SQL 相似。现有 `readIdempotentChange/storeIdempotentChange` 与 `CoreTransactionSideEffects.audit` 已覆盖稳定共性，继续做高阶服务抽象会隐藏差异。
- 修改：`PostgresPaymentRepository` 的“新 PAID 事件命中已支付订单”与“新退款事件累计值未变化”两分支复用私有 `markEventDuplicate`；两者保持同一事务、同 inbox 更新、同 wallet 行锁、同返回，且均不写账本/订单/成功审计。相同 provider event ID 原文重放仍保持独立，返回 `walletBalanceCents=null`。
- 补强：人工对账终态现在通过既有 `TransactionSideEffects` 在状态更新同一事务写 `PAYMENT_RECONCILIATION_REQUIRED` 失败审计，保留错误码、requestId 与非敏感前后态。
- 验证：`payment-duplicate-events.test.ts` 4 个反例通过，覆盖两种业务重复、provider event 原文重放隔离和对账审计；未把 shop/membership/auth 的异义流程泛化。

### SHOP-EXP-001 / SHOP-IDEM-001：到期授权与收费请求绑定

- 到期安全：对象级授权 SQL 以 `Asia/Shanghai` 当地 `close_date` 派生有效 `EXPIRED_READONLY`，直接调用上传、确认、发布等 API 不再依赖先访问店铺列表。持久 `ACTIVE → EXPIRED_READONLY` 改为条件 `UPDATE ... RETURNING` 与逐店 `SHOP_EXPIRED_READONLY` 系统审计同事务；并发空返回不会重复审计，审计失败会回滚状态。
- 店铺幂等：create 重放绑定 CREATE 动作、application、owner、规范初始店名、start/close 与管理员减免；renew 绑定 RENEW 动作、shop、目标 close 与减免。利用现有 shop/term/charge/name-history 证据，无迁移；跨动作或任一目标变化稳定返回 `IDEMPOTENCY_KEY_REUSED` 409。
- 价格幂等：`effectiveFrom` 规范到 UTC ISO 毫秒精度，重放同时比较 application、金额、生效 instant 与 trim 后 reason；旧数据缺少对应价格审计时安全冲突。
- 验证：店铺/价格/对象授权 7 文件/38 用例通过；到期审计 2 文件/16 用例通过；全量测试检查点为 54 文件/195 用例通过、6 文件/15 用例跳过，typecheck 通过。真实 PostgreSQL 分支仍未执行。

### INV-EXP-001：邀请过期成为持久事实

- 修改：重邀前在店铺锁事务内把同店铺/手机号的自然过期 PENDING 邀请更新为 EXPIRED 并逐条写审计，释放部分唯一索引；accept 过期 token 先提交状态和审计，再在事务外返回 `INVITATION_INVALID`，避免抛错回滚过期事实。
- 构造安全：`MembershipArtifactInvalidator` 改为必填，删除会让未来构造点静默跳过撤权失效的默认 no-op；全仓零调用的 `lookupActive` 已删除。
- 验证：2 个定向用例覆盖重邀、相同新键重试不重复创建/审计、过期 accept 的提交顺序；数据库集成套件因无测试库保持跳过。

### EXP-001 第一阶段：保持流式边界并修复生命周期

- 拒绝宽拆分：XLSX、CSV、ZIP 共享单次 RowSource 消费、行数守恒与 manifest；授权还绑定 requester、membership epoch 与任务状态；不抽成通用 repository/stream 模板，也不新增第二套业务租约。
- 对象安全：工作区明文清理与密文清理解耦；删除密文前先查询 `stored_object` 引用，查询失败或证据缺失时宁可保留孤儿。终态 fail、SUCCEEDED 与 COMMIT 响应不确定均有回归。
- 撤权竞态：create 在同一事务锁 membership/shop、以最新 epoch 授权后插入，和 revoke 的行锁/扫描串行化，消除撤权完成后再插入旧 epoch QUEUED 的窗口。
- 导出一致性：XLSX/CSV 共用 decimal 最多 8 位与 integer 校验；manifest 文件显式携带逻辑文件名/mediaType，直接 XLSX 固定为 `report.xlsx`；FX 使用 `(marketplace, local_month, id)` 复合 keyset，继续 10,000 行分页并保证 CSV 分区连续。
- 验证：7 个导出文件/18 用例通过，typecheck 和定向 lint 通过；复合 keyset 未在真实 PostgreSQL 执行或 EXPLAIN，双连接撤权竞争仍缺集成环境证据。

### EXP-001 第二阶段：一次性下载授权、审计与容量门禁

- 下载授权：新增前向迁移 `0024_export_download_grants.sql`；32 字节随机 token 仅持久化 SHA-256，5 分钟、一次性、可撤销。下载消费在事务中重新校验当前店铺、请求人、membership epoch、导出状态与 grant 状态；撤权事务同时使未消费的旧 epoch grant 失效。明文 token 不进入 SQL、审计或日志。
- 审计：create、幂等 replay、cancel、cancel no-op、token 创建、下载授权、Worker 生成成功与终态失败均使用明确 action/result/requestId；系统生成使用稳定 `system:export.generate:<exportId>`，失败审计不记录异常原文或导出内容。
- 容量：生成前以目标卷 `statfs` 证据执行 `2 × 保守预计产物 + 2GiB` 门禁；证据不可用即停止。生产列投影都声明写出边界，Marketplace 120 字符按最多 512 UTF-8 字节估算并在 writer 同时强制，避免估算与实际输出漂移。
- 架构保护：继续使用 pg-boss heartbeat 作为唯一租约，未新增业务 lease；XLSX/CSV writer、复合 keyset、对象提交与失败清理仍是流式/幂等路径。已提交撤权对象的延迟物理清理没有可复用调度基础设施，因此未伪造清理器。
- 验证：导出与相关路由定向 14 文件/35 用例通过；更宽导出审查为 15 文件/36 用例通过；typecheck 与定向 lint 通过。迁移、双连接竞争、目标生产卷容量演练仍未执行。

### AUDIT-001：补齐已证实的高价值审计边界

- 认证：成功登录的 session 与 `ACCOUNT_LOGIN/SUCCEEDED` 同事务；失败登录在 OTP 事务提交后持久写 `ACCOUNT_LOGIN/FAILED`，审计不可用统一 fail-closed 为 `AUTH_AUDIT_UNAVAILABLE`；手机号换绑、会话撤销与 `ACCOUNT_PHONE_CHANGED` 同事务。手机号、OTP、session token、CSRF 摘要均不进入审计。
- 支付：人工对账终态在状态更新同一事务写 `PAYMENT_RECONCILIATION_REQUIRED` 失败审计。
- 导出：覆盖请求、幂等重放、取消/no-op、token、下载授权、生成成功与终态失败，并保留 request ID 与非敏感前后态。
- 财务访问：管理员读取正式或试算结果时，先完成对象级授权与成功取数，再在返回响应前通过现有 `CoreTransactionSideEffects` 写 `ADMIN_FINANCIAL_REPORT_VIEWED`；普通用户不触发，审计失败不会把结果返回给客户端。
- 验证：认证定向 4 文件/19 用例通过；管理员结果访问与路由边界 2 文件/9 用例通过；支付与导出证据见对应切片。OTP 申请/限流、注册失败、注销、会话失效、CSRF 拒绝及换绑失败属于后续低优先审计扩展，不作为本轮已证实高/中价值缺口。

### CLEAN-001：删除零调用实现并移除生产静默回退

- 删除：`src/shared/failure-injection.ts`，以及零调用的 dataset 激活、mapping version 创建、FX `convertAmount`、operations `sha256File`、auth `revokeAllSessions`、membership `lookupActive`、通用 schema/error/health/helper 等实现；保留仍被脚本实际调用的局部 `sha256File`。
- 收紧：imports 路由的 `getCompleteness` 与 membership 的 artifact invalidator 改为必填依赖，不再以可选 callback/default no-op 静默跳过生产能力。
- 验证：目标符号在 `src/tests/scripts` 中无残留；清理相关 5 文件/19 用例通过，且进入最终全门禁。

### 最终对抗复核：第二轮反例

- 全新上下文只读复核证实六个此前门禁未覆盖的反例：Linux 下导出工作目录使用反斜杠而落到错误 POSIX 路径；已消费但过期的双 OTP 仍可换绑；合法 `numeric(30,8)` 经 Excel `VALUE` 超过 15 位有效数字后失真；membership 幂等重放先于当前授权；同值导出权限无效旋转 epoch；导出终态持久化并返回原始异常。
- 已完成修复检查点：手机号换绑完成事务读取并强制比较两份 challenge 的 `expires_at`；membership 的授权前移到幂等读取之前，同值权限成为不更新、不失效、不审计的持久 no-op。
- 导出修复：API/Worker 工作目录统一使用平台 `path.join`；原始异常写入和历史脏 `error_code` 返回均归一到白名单稳定码；XLSX 仅接受 Excel 可保持的至多 15 位有效数字，超过边界明确失败而不近似，行数溢出 CSV 保留原字符串；`factId` 作为标识符按文本导出。
- 相邻复核发现：既有 ACTIVE membership 接受新邀请会旋转 epoch，却未失效旧 epoch 导出。accept 现于同一事务锁定旧 membership，epoch 变化后调用既有失效器，使旧 `QUEUED/RUNNING/SUCCEEDED` 导出与未消费 grant 一并失效。
- 六项修复的组合定向回归为 17 文件/45 用例通过；最终独立复核在当前源码上重放六项和相邻 accept 场景，6 文件/22 用例通过，相关 lint 0 error、typecheck 通过，未发现新的高/中价值正确性问题。

## 收口验证检查点

采集时间：2026-07-28 13:45（Asia/Shanghai）。

| 指标 | baseline | 当前 | 差异 |
|---|---:|---:|---:|
| `src` TS/Vue/CSS/HTML 文件 | 126 | 126 | 0 |
| 总字节 | 635,233 | 667,001 | +31,768 |
| 物理行 | 12,641 | 13,602 | +961 |
| 非空行 | 11,841 | 12,791 | +950 |
| 行长 > 120 / 160 / 300 / 1000 | 690 / 284 / 67 / 4 | 700 / 276 / 57 / 0 | +10 / -8 / -10 / -4 |
| Web JS/CSS/HTML 构建字节 | 400,627 | 400,127 | -500 |
| Web 全部构建字节（含图片、map） | 3,214,487 | 3,218,868 | +4,381 |
| API entry / Worker entry / Server shared chunk | 174,410 / 40,848 / 148,395 | 185,136 / 41,133 / 165,110 | +10,726 / +285 / +16,715 |
| Server JS 总字节 | 363,653 | 391,379 | +27,726 |

- 体积解释：事务/认证/授权/前端映射/幂等 helper 等精简形成局部净减少，但本轮同时修复筛选、请求边界、竞态、审计、下载授权、容量门禁和持久过期等已证实正确性缺口，因此最终源文件与 Server 产物净增加；不把正确性实现伪称为精简收益。Web 代码构建产物相对基线仍净减少 500 字节。
- 最终全门禁在最后一次源码修改后重新执行：63 文件/220 用例通过，6 文件/15 用例因环境条件跳过；lint 0 error/864 warning；typecheck、Web build、Server build 全部通过。原始输出：`.work/code-optimization/final-verify-rerun2.log`，SHA-256 `7376a68da66a240a6e1ba85f2376f92e63c7c0498d7aef3f52c58be6deee1fad`。

## 未解决风险

- 15 个测试用例与 6 个测试文件因环境条件跳过，数据库、恢复、导出和大文件结论不能由当前基线全量证明。
- `service-graph` 授权路径已由可计数测试固定为一次查询，但仍未在隔离 PostgreSQL 中运行 `EXPLAIN (ANALYZE, BUFFERS)`。
- REPORT-001 的事实级聚合 SQL 只有可计数/查询契约与现有财务 golden 保护；缺少隔离 PostgreSQL 的真实执行与执行计划。
- `0024_export_download_grants.sql` 未应用；短期令牌、撤权实时校验与容量门禁只有注入式/可计数测试，尚无真实双连接竞争或目标卷演练。撤权后已提交对象的延迟物理清理仍缺生命周期基础设施。
- Excel/LibreOffice 的数值模型不能精确重算超过 15 位有效数字的 `numeric(30,8)`；当前 XLSX 对此失败关闭，只有已经因行数超限进入 ZIP 的对应 CSV 会保留该极值。未实现为仅因精度自动切换 ZIP 的预扫描，因为这会增加全量双遍查询并改变现有流式契约。
- OTP 申请/限流、注册失败、注销、会话失效、CSRF 拒绝和手机号换绑失败尚无完整业务审计；当前未发现它们构成本轮高/中价值正确性阻断。
- Web 尚有 864 条排版 warning；本轮仅处理可与行为路径隔离且有编译等价证据的窄范围。
- 当前工作区没有 Git 元数据；恢复依赖只读快照与哈希清单。

## next_action
